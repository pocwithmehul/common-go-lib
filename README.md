# common-go-lib
Common go library code for various services to be implemented using golang. The library will have common code for locale, parsing token, wrapper for kafka, mongodb, dynamodb, postgreSQL, logging etc.
